# 贡献指南

感谢你帮助建设 `Awesome-VibeCoding-Showcase`。本仓库展示由 AI 实质性参与开发、已经能够公开体验且源码公开的软件成果。

简体中文是主维护语言。英文贡献同样欢迎；关键英文内容维护在 `CONTRIBUTING.en.md`，项目字段本身采用中英可理解的固定格式。

## 1. 先确认范围

本项目的建设范围仅限 GitHub 仓库，不为本 Showcase 开发独立官网。候选产品自身的公开体验地址不受此限制。

可以提交：

- 最终可运行、可公开体验的软件产品；
- 产品主体源码公开；
- AI 通过自然语言协作承担了大部分核心功能实现；
- 信息真实，并有足够公开证据供维护者核验。

不要提交：

- AI 编程工具、IDE、编辑器、插件或模型；
- 组件库、模板、Starter、Prompt 合集、教程或资源清单；
- 只有截图、设计稿、概念说明或宣传视频，无法公开体验的项目；
- 源码未公开，或仓库仅含落地页、文档、编译产物的项目；
- AI 仅用于文档、注释、测试补全、少量 bug 修复或素材生成的项目。

## 2. 收录门槛

五项标准必须同时满足：

| 编号 | 必须满足的条件 | 最低核验材料 |
| --- | --- | --- |
| C1 | 有可公开访问的产品、演示或在线体验 | 可由审核者实际打开并验证核心流程的 URL |
| C2 | 有公开的源码仓库 | 包含产品主体实现且无需授权即可查看的仓库 URL |
| C3 | AI 实质性参与产品实现 | AI 工具、参与深度、典型协作方式，以及至少一种公开过程证据 |
| C4 | 具备一定完成度、实用性或创新性 | 可复现的核心流程，以及支持至少一个质量维度的证据 |
| C5 | 信息真实、可验证 | 产品、源码、提交人说明和证据之间能够相互印证 |

详细判定口径与不通过示例见 `docs/CRITERIA.md`。

## 3. 每个项目必须提供的信息

| 编号 | 展示字段 | 填写要求 |
| --- | --- | --- |
| D1 | 产品解决的问题 | 说明目标用户、使用场景和具体问题 |
| D2 | 核心功能与实际价值 | 只描述已经可用的功能，并说明实际价值 |
| D3 | 主要技术栈 | 列出构成产品主体的主要前端、后端、数据与部署技术 |
| D4 | AI 的开发角色 | 说明工具、参与深度、典型自然语言协作方式和公开证据 |
| D5 | 在线体验与源码地址 | 两个地址都必须提供并可公开访问 |

展示格式见 `docs/PROJECT_FORMAT.md`。

## 4. 如何贡献

### 4.1 提名新项目

1. 搜索已有项目与开放 Issue，确认没有重复条目。
2. 阅读 `docs/CRITERIA.md` 和 `docs/CATEGORIES.md`。
3. 打开“项目提交 / Project submission”Issue Form。
4. 完整填写 C1–C5 与 D1–D5，不要只粘贴项目宣传文案。
5. 提交后订阅该 Issue；若维护者标记 `status: needs-info`，请在同一 Issue 补充证据。

新项目无需先提交 PR。维护者确认资格和展示内容后，再创建或邀请对应的内容 PR，以减少无效修改。

### 4.2 报告错误、失效链接或建议下架

1. 使用“通用问题 / General issue”表单。
2. 选择对应问题类型并给出项目名称、条目位置和公开证据。
3. 若涉及项目事实变化，按适用范围重新提供 C1–C5 / D1–D5 信息。
4. 安全漏洞不要公开披露利用细节；只说明受影响条目，并使用仓库启用的私密安全报告渠道。

### 4.3 更新已有项目

项目作者或社区成员都可以建议更新，但事实性修改必须有公开来源。以下变化应主动报告：

- 在线体验或源码地址变化（C1 / C2 / D5）；
- 核心功能、价值或技术栈变化（D1–D3）；
- AI 开发证据获得更可靠的公开来源（C3 / C5 / D4）；
- 项目停止运行、源码转为私有或信息被证实不实（C1 / C2 / C5）。

### 4.4 提交 Pull Request

1. 一个 PR 只处理一个明确目的。
2. 新增项目必须关联带有 `status: approved` 的项目提交 Issue；文档、翻译、配置等非项目变更可说明为什么不需要 Issue。
3. 使用 PR 模板；涉及项目时完整填写 C1–C5 / D1–D5。
4. 按 `docs/CATEGORIES.md` 选择一个一级和一个二级分类，并在唯一事实源 `data/projects.json` 中维护条目；不要直接编辑 README 的目录生成区。
5. 运行 `python scripts/render_catalog.py`，将生成后的 `README.md` 与 `README.en.md` 一并提交。
6. 实际打开体验与源码链接，记录验证过的核心流程。
7. 同步受影响的中文主内容和关键英文内容。
8. 运行下方本地检查，不要夹带无关重排、批量格式化或工具升级。

```powershell
python scripts/validate_catalog.py
python scripts/validate_repository.py
python scripts/render_catalog.py --check
python -m unittest discover -s scripts -p "test_*.py"
```

本地检查验证字段、格式与审核 Issue 链接结构；正式 PR 的 CI 还会把审核链接绑定到当前仓库，并确认对应 Issue 真实存在且带有批准标签。

结构化字段说明和示例见 `data/README.md`；展示结果应符合 `docs/PROJECT_FORMAT.md`。

## 5. 证据要求

可接受的公开证据包括：

- 项目仓库中的开发说明或 AI 协作记录；
- 与核心功能实现有关的公开 Issue、Pull Request 或提交说明；
- 公开开发日志、作者技术复盘或经脱敏的过程记录；
- 可重现的产品操作步骤、公开文档和版本说明。

以下内容不能单独证明 C3 或 C5：

- 仅声明“由 AI 构建”；
- 只列出 AI 工具名称；
- 代码风格看起来像 AI 生成；
- 无法核验来源的截图、Star 数、用户数或宣传语；
- 私有聊天记录或要求审核者加入私密群组后才能看到的证据。

公开过程证据前请遮盖 API Key、访问令牌、账号、私人对话和个人身份信息。

## 6. 分类规则

- 按产品主要解决的问题分类，而不是按所用技术或 AI 工具分类。
- 每个项目只选择一个一级分类和该一级分类下的一个二级分类。
- 多用途产品按官网或文档明确的首要目标用户与核心流程决定主分类。
- 无法机械判断的边界情况按 `docs/CATEGORIES.md` 的逐类规则处理，并在 Issue 中记录理由。

## 7. 中英文协作

- 中文内容是当前事实与治理规则的主版本。
- 涉及定位、收录标准、提交方式、项目关键字段的变更，必须在同一 PR 同步英文版本。
- 若贡献者暂时无法完成英文翻译，可在 PR 中明确标注并添加 `i18n` 标签；不应复制未经复核的机器翻译作为最终文本。
- 项目名、URL、代码、标签与分类 slug 不翻译。

## 8. 审核结果

维护者使用 `docs/REVIEW_CHECKLIST.md` 记录判断。状态流为 `triage → needs-info | in-review → approved → listed`；现有条目复核时可进入 `stale → removed`。关键结果为：

- `status: approved`：C1–C5 全部通过，D1–D5 完整；
- `status: needs-info`：证据不足但可合理补充；
- `status: declined`：任一强制条件不满足，或补充后仍无法核验；
- `status: removed`：已收录项目经复核后下架。

自动化检查通过只代表链接、格式或字段完整性没有发现问题，不替代对 C3–C5 的人工判断。

## 9. 协作要求

- 就事论事，使用可复核证据，不以作者知名度、商业关系或项目热度替代标准。
- 主动披露自己与候选项目的作者、雇佣、投资或推广关系。
- 接受维护者为保持一致性而编辑描述、分类与措辞。
- 不刷屏、不重复催促，不以赠品、互推或付费换取收录。

提交贡献即表示你有权公开所提供的内容，并同意其按仓库许可证分发。
